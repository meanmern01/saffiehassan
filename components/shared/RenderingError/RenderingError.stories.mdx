import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import RenderingError from './RenderingError';

<Meta title="WIP/RenderingError" component={RenderingError} />

export const Template = (args) => <RenderingError {...args} />;

# RenderingError

Компонент ошибки рендеринга.

## Props

<ArgsTable of={RenderingError} />

## Default

<Canvas>
    <Story name="Default" args={{ message: 'An error occured' }}>
        {Template.bind({})}
    </Story>
</Canvas>
