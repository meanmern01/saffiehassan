import { Meta, Canvas, Source } from '@storybook/addon-docs';
import ComponentProps from '@/.storybook/components/ComponentProps';
import LoadingScreen from './LoadingScreen';

<Meta title="Components/LoadingScreen/Overview" component={LoadingScreen} />

# LoadingScreen

LoadingScreen may be shown to overlay the app when some asynchronous action is taking place.

<Canvas withToolbar>
    <div
        style={{
            position: 'relative',
            transform: 'translate(0, 0)',
            width: 1920,
            height: 700,
            maxWidth: '100%',
        }}
    >
        <LoadingScreen visible text="Preparing your analysis..." />
    </div>
</Canvas>

<ComponentProps component={LoadingScreen}>
    <p>Supports all the default HTML "div" attributes.</p>
</ComponentProps>

## Import

<Source
    language="ts"
    format={false}
    code={`
        import LoadingScreen from '@/components/shared/LoadingScreen';
  `}
/>
